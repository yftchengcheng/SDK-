# 频次设置 — 需求规格

> 来源：参考图 `assets/应用管理-频次控制.png`
> 适用范围：Taku SDK v6.4.50 及以上，应用管理 / 应用详情

---

## 1. 入口

- 触发位置：应用详情头部（`/app`），右上角操作区「频次设置」按钮
- 打开方式：**右侧侧边抽屉**（`el-drawer direction="rtl"`），宽度 ~560px
- 标题：「频次设置」
- 顶部说明文案：「Taku SDK v6.4.50 及以上版本，支持在 APP 维度设置每台设备上的广告平台或广告样式频次」

---

## 2. 4 个规则模块（自上而下）

每个模块是一个独立的「白底圆角卡片」，可单独添加多条规则。

| # | 名称 | 维度 | 单位 | 输入项 | 默认 |
|---|------|------|------|--------|------|
| ① | 展示上限（天） | 天 | 次 | 次数（不限/N）、广告平台、广告类型 | 全部不限 |
| ② | 展示上限（小时） | 小时 | 次 | 次数（不限/N）、广告平台、广告类型 | 全部不限 |
| ③ | 展示间隔（秒） | 秒 | 秒 | 间隔秒数（不限/N）、广告平台、广告类型 | 全部不限 |
| ④ | 请求上限 | 自定义时间周期 | 次 + 秒 | 次数（次）、时间周期（秒/每 X 秒）、广告平台、广告类型 | 全部不限 |

### 2.1 单条规则 UI 结构（模块内每条规则）

```
┌─ 规则 #N                            [×] ─┐
│                                          │
│  次数   [不限  |   3  ] 次              │
│  广告平台 [全部 ▾]                       │
│  广告类型 [全部 ▾]                       │
│                                          │
│  💡 广告展示上限为不限制（实时提示）     │
└──────────────────────────────────────────┘
[+ 添加规则]
```

**特殊：模块 ④「请求上限」两个数值输入框**：
```
次数  [不限  |   5  ] 次       时间  [每] [不限  |   30  ] 秒
```

### 2.2 控件规格

| 控件 | 样式 | 行为 |
|------|------|------|
| 数值输入 | 圆角白底 + 内嵌下拉「不限」+ 单位后缀 | 「不限」选中时输入框 disabled，显示占位 |
| 平台下拉 | el-select | 选项：全部 / 穿山甲 / 优量汇 / 快手 / 百度 / 自定义平台... |
| 类型下拉 | el-select | 选项：全部 / 横幅 / 插屏 / 开屏 / 原生 / 视频 / 激励视频 |
| 删除按钮 | 卡片右上角 × 图标（ElIcon Close） | 删除该条规则 |
| 添加规则 | 卡片底部「+ 添加规则」文字按钮（primary color） | 在该模块末尾追加一条空规则 |
| 提示语 | 模块底部行，slate-400 12px，💡 emoji 可选 | 实时显示「X 为不限制」或「X 为 N 次/秒」 |

---

## 3. 数据结构（前端 form）

```ts
interface FrequencyRule {
  id?: string;                   // 客户端临时 key（删除/编辑用）
  count: number | null;          // null = 不限
  platforms: string[];           // ['all'] | ['pangaea', 'gdt', ...]
  adTypes: string[];             // ['all'] | ['banner', 'interstitial', ...]
}

interface FrequencyConfig {
  // 模块 ①
  impressionCapDay: FrequencyRule[];
  // 模块 ②
  impressionCapHour: FrequencyRule[];
  // 模块 ③
  impressionInterval: FrequencyRule[];
  // 模块 ④（多了 timeWindow）
  requestCap: Array<FrequencyRule & { timeWindow: number | null /* 秒 */ }>;
}
```

### 3.1 后端存储

| 字段 | 表 | 字段 | 类型 |
|------|------|------|------|
| 配置 | `app` | `frequency_config JSONB` | 存上面 4 个数组 |
| 生效 | SDK 端 | 通过 OpenAPI 拉取 → 本地执行 | — |

### 3.2 API 端点

- `GET /api/v1/console/app/[id]/frequency` — 读取频次配置
- `PUT /api/v1/console/app/[id]/frequency` — 保存频次配置

---

## 4. 底部按钮

```
[取消]   [确定]
```

- 取消：plain 次要按钮（白底灰边），关闭抽屉不保存
- 确定：primary 主要按钮（蓝底白字），保存并关闭
- 校验：4 个模块的规则数 ≤ 10；至少 1 个模块 count 非 null 才允许保存（否则提示「请至少配置一条规则」）

---

## 5. 视觉规范（遵循 DESIGN.md）

- **容器**：白底 + 1px slate-200 边框 + radius 8px + shadow-sm
- **模块间距**：模块之间 16px
- **模块内 padding**：20px
- **标题**：16px / 600 权重 / slate-900
- **label**：13px / 500 权重 / slate-700
- **下拉/输入框**：高度 36px
- **删除按钮**：hover 红色
- **提示行**：12px / slate-400 / 12px top margin
- **底部按钮区**：48px 高度，右对齐，gap 12px

---

## 6. 边界 & 异常

| 场景 | 行为 |
|------|------|
| 某模块 0 条规则 | 显示「+ 添加规则」空态，不显示卡片主体 |
| 全部模块都空 | 提示「请至少配置一条规则」红字 |
| count 输入 0 | 自动转「不限」 |
| count 输入负数 | 阻止输入 |
| 平台/类型选「全部」 | 数组存为 `['all']`，后端识别为不过滤 |
| 删除最后一条规则 | 整个模块从 form 移除（不留空数组） |

---

## 7. 实施 TODO（建议拆解）

- [ ] 1. 新增 `FrequencyDrawer.vue` 组件（4 模块 + 规则增删 + 提示行）
- [ ] 2. 数据结构 + 校验规则（参考 3.1 / 6）
- [ ] 3. 新增 API 路由（GET / PUT frequency）
- [ ] 4. Supabase 表 `app.frequency_config` JSONB 字段迁移
- [ ] 5. 应用详情头部加「频次设置」按钮 → open drawer
- [ ] 6. DESIGN.md 增补「频次设置组件规范」token
- [ ] 7. 冒烟测试：保存 4 种规则 → 重启 SDK 端可拉取
